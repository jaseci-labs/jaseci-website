# Environment variables for jaseci-org
# Copy this file to .env.local and fill in values for local development
# These NEXT_PUBLIC_ variables are exposed to the browser by Next.js

# Google Analytics 4 Measurement ID, e.g. G-XXXXXXXXXX
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Mailchimp: find your API key in Mailchimp -> Account -> Extras -> API keys.
# Format: <key>-<dc>, e.g. 0123456789abcdef-us15
MAILCHIMP_API_KEY=

# Audience (List) ID: find under Audience -> Settings -> Audience name and defaults.
MAILCHIMP_AUDIENCE_ID=

# Data center slug only (no protocol, no domain), e.g. "us15".
# If omitted or malformed, the API tries to derive it from MAILCHIMP_API_KEY suffix.
MAILCHIMP_API_SERVER=us15

# Double opt-in: when true, users receive a confirmation email and must confirm
# before being added. When false, users are subscribed immediately.
# Accepts: "true" or "false" (case-insensitive). Default is false.
MAILCHIMP_DOUBLE_OPT_IN=true